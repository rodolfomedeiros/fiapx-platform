# fiapx-platform

Infraestrutura de desenvolvimento local e contratos compartilhados.

```sh
docker compose up --build
```

Entrada pública: `http://localhost:8080`; RabbitMQ: `http://localhost:15672`; MinIO: `http://localhost:9001`; Mailpit: `http://localhost:8025`.
